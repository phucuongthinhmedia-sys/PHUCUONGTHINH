[phases.install]
cmds = ["cd packages/backend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd packages/backend && npm run build"]

[start]
cmd = "cd packages/backend && npx prisma migrate deploy && node dist/src/main"
